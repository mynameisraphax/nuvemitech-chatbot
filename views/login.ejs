<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - NuvemITech CRM</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
    <img src="/logo.png" alt="NuvemITech Logo" class="h-10 mx-auto mb-6">
    <h1 class="text-2xl font-bold mb-6 text-center">Login - NuvemITech CRM</h1>
    <p id="error-message" class="text-red-500 mb-4 text-center hidden"></p>
    <% if (error) { %>
      <p id="initial-error" class="text-red-500 mb-4 text-center"><%= error %></p>
    <% } %>
    <form id="login-form" action="/login" method="POST">
      <div class="mb-4">
        <label class="block text-gray-700">Usuário</label>
        <input type="text" name="username" required class="w-full p-2 border rounded">
      </div>
      <div class="mb-6">
        <label class="block text-gray-700">Senha</label>
        <input type="password" name="password" required class="w-full p-2 border rounded">
      </div>
      <button type="submit" class="w-full bg-blue-600 text-white p-2 rounded hover:bg-blue-700">Entrar</button>
    </form>
  </div>
</body>
</html>